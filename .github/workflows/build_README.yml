name: Build command index
on:
  push:
    branches:
      - main

jobs:
  build-index:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Build index
        run: |
          bash build/extract_commands.sh

      - name: Publish index
        uses: actions/upload-artifact@v2.2.5
        with:
          name: commands
          path: commands.md
